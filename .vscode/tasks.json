{
	"version": "2.0.0",
	"tasks": [
		{
			"type": "shell",
			"label": "deploy",
			"command": "rsync -r ${workspaceFolder}/build/install/arm/ root@${config:target_ip}:/usr/local/",
			"problemMatcher": []
		}
	]
}